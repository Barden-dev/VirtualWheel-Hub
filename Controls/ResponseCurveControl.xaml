<UserControl x:Class="SimRacingHub.Controls.ResponseCurveControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="300" Height="180"
             SizeChanged="UserControl_SizeChanged">
    <Border Background="{DynamicResource ControlFillColorSecondaryBrush}" 
            BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}" 
            BorderThickness="1" 
            CornerRadius="6" 
            Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Canvas x:Name="PlotCanvas" Grid.Row="0" ClipToBounds="True" Background="Transparent">
                <!-- Background Grid Lines -->
                <Path x:Name="GridPath" Stroke="#33FFFFFF" StrokeThickness="1" StrokeDashArray="2 2" />
                
                <!-- Linear Reference Line (1:1) -->
                <Path x:Name="LinearPath" Stroke="#44FFFFFF" StrokeThickness="1" StrokeDashArray="4 4" />
                
                <!-- Non-Linear Gamma Curve -->
                <Path x:Name="CurvePath" Stroke="{Binding CurveColor, RelativeSource={RelativeSource AncestorType=UserControl}}" StrokeThickness="2.5" />
                
                <!-- Live Input Indicator Dot -->
                <Ellipse x:Name="LiveDot" Width="10" Height="10" Fill="#F59E0B" Stroke="#FFF" StrokeThickness="1.5">
                    <Ellipse.Effect>
                        <DropShadowEffect Color="#F59E0B" BlurRadius="8" ShadowDepth="0" Opacity="0.8"/>
                    </Ellipse.Effect>
                </Ellipse>
            </Canvas>

            <!-- Bottom Data Readout with explicit Column Definitions -->
            <Grid Grid.Row="1" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TxtInput" Grid.Column="0" Text="In: 0%" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" HorizontalAlignment="Left"/>
                <TextBlock x:Name="TxtGamma" Grid.Column="1" Text="γ = 1.00" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" HorizontalAlignment="Center" FontWeight="SemiBold" Margin="4,0"/>
                <TextBlock x:Name="TxtOutput" Grid.Column="2" Text="Out: 0%" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" HorizontalAlignment="Right"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
